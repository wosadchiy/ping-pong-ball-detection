opencv-python>=4.8
numpy>=1.24
pyserial>=3.5

# SPI master link Pi 4 → STM32 Nucleo (firmware/.../tools/spi_test.py and the
# future Stm32SpiHandler). Linux-only: spidev wraps the kernel /dev/spidevX.Y
# ioctl interface, which doesn't exist on Windows/macOS. The marker keeps
# `pip install -r` working on dev laptops (it just won't install spidev there).
spidev>=3.6 ; sys_platform == "linux"

# DearPyGui versioning is OS-specific:
#   * 1.x is the last branch that boots reliably on macOS (Apple Silicon).
#     The 2.x viewport closes immediately after `create_viewport()` on Sonoma+
#     (known upstream issue), so we MUST pin <2 there.
#     Wheels: cp38..cp312 only — i.e. macOS users need Python 3.12 max.
#   * 2.x is fine on Windows / Linux and provides wheels for Python 3.13+.
#     Pinning <2 there breaks installs on modern Pythons.
dearpygui>=1.10,<2 ; sys_platform == "darwin"
dearpygui>=2.0    ; sys_platform != "darwin"

taskipy>=1.12
pyinstaller>=6.0
